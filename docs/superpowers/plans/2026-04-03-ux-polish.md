# UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply 12 targeted UX improvements to make the FCV Project Screener more professional and World Bank-aligned.

**Architecture:** All changes are in a single file (`index.html`) across CSS, HTML, and JS sections. No backend changes. Each task is a self-contained edit that can be verified in the browser.

**Tech Stack:** HTML, CSS, vanilla JavaScript (inline in `index.html`)

---

### Task 1: Custom Styled File Upload Zones

**Files:**
- Modify: `index.html:239-260` (CSS — upload zone styles)
- Modify: `index.html:1812-1832` (HTML — upload zone markup)

- [ ] **Step 1: Hide native file inputs and style the drop zones**

In the CSS section (~line 260), after the existing `.drop-zone` rules, the native file inputs are already hidden with `input[type=file]{display:none}` but the upload zones at lines 1820 and 1830 have inline `style="display:block"` overriding this. Fix by removing the inline styles and adding a styled browse button.

Replace the project document drop zone inner content (line 1816–1821):
```html
    <div class="drop-zone" id="dp">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" style="color:var(--border)" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
      <p style="margin-bottom:4px">Drag &amp; drop, or</p>
      <button type="button" class="browse-btn" onclick="document.getElementById('ip').click()">Browse files</button>
      <input type="file" id="ip" name="project_doc" multiple accept=".pdf,.txt,.docx,.doc,.md">
    </div>
```

Replace the contextual documents drop zone inner content (line 1826–1831):
```html
    <div class="drop-zone" id="dc">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" style="color:var(--border)" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="9" y1="21" x2="9" y2="9"/></svg>
      <p style="margin-bottom:4px">Drag &amp; drop, or</p>
      <button type="button" class="browse-btn" onclick="document.getElementById('ic').click()">Browse files</button>
      <input type="file" id="ic" name="context_doc" multiple accept=".pdf,.txt,.docx,.doc,.md">
    </div>
```

- [ ] **Step 2: Add CSS for the browse button**

After the `.drop-zone` CSS rules (~line 260), add:
```css
.browse-btn{background:var(--wbg-blue);color:#fff;border:none;border-radius:var(--radius-sm);padding:6px 16px;font-size:12px;font-weight:600;cursor:pointer;font-family:inherit;letter-spacing:.03em;transition:background .15s}
.browse-btn:hover{background:#0080c0}
```

- [ ] **Step 3: Verify in browser**

Open the app in Chrome. Both upload zones should show an upload icon, "Drag & drop, or" text, and a styled blue "Browse files" button. No raw browser file input visible. Clicking "Browse files" should open the file picker. Drag-and-drop should still work.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat: style file upload zones with custom browse button"
```

---

### Task 2: Fix Mode Card Badges

**Files:**
- Modify: `index.html:1856-1857` (HTML — Step-by-Step mode card badge)

- [ ] **Step 1: Remove the RECOMMENDED badge from Step-by-Step card**

Find line 1857:
```html
      <div class="mode-badge mode-badge-rec">Recommended</div>
```

Replace with an empty string (remove the line entirely). The Express card keeps its "DEFAULT" badge unchanged.

- [ ] **Step 2: Verify in browser**

Open the app. Express should show "DEFAULT" badge. Step-by-Step should show no badge — just the title, description, and time estimate.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: remove conflicting RECOMMENDED badge from step-by-step mode card"
```

---

### Task 3: Trim Hero Text

**Files:**
- Modify: `index.html:1692` (HTML — hero description paragraph)

- [ ] **Step 1: Replace the hero paragraph**

Find line 1692:
```html
<p style="font-size:14px;color:rgba(255,255,255,.82);line-height:1.65;max-width:600px;margin-bottom:18px;">This tool uses an LLM to screen draft WBG project documents for FCV dynamics and generate recommendations on how to strengthen FCV sensitivity and responsiveness. Upload your project documents and follow the step-by-step process to produce a final recommendation note. At each stage you can review the AI's thinking and make refinements before proceeding.</p>
```

Replace with:
```html
<p style="font-size:14px;color:rgba(255,255,255,.82);line-height:1.65;max-width:600px;margin-bottom:18px;">Screen draft WBG project documents for FCV dynamics and generate actionable recommendations to strengthen sensitivity and responsiveness. Upload your documents and review the AI's analysis at each stage.</p>
```

- [ ] **Step 2: Verify in browser**

Hero section should show 2 concise sentences instead of 3 verbose ones.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: condense hero paragraph to 2 sentences"
```

---

### Task 4: Clean Up Session Bar

**Files:**
- Modify: `index.html:1665` (HTML — session status span)

- [ ] **Step 1: Hide "No active session" on initial load**

Find line 1665:
```html
  <span id="session-status">No active session</span>
```

Replace with:
```html
  <span id="session-status" style="display:none">No active session</span>
```

This hides the text initially. The JS that updates `session-status` (e.g., when a session is saved/loaded or doc type is detected) already sets `textContent` — it will also need to set `display:inline` when content is meaningful. Find where `session-status` is updated in JS and ensure it becomes visible when set to a real value.

- [ ] **Step 2: Make session-status visible when updated**

Search JS for `session-status`. There is already logic that sets it (e.g., when loading a session or setting session name). Add `el.style.display='inline'` after any `textContent` update that sets a meaningful value. Specifically, in the `updateSessionBar()` function or wherever `session-status` text is set.

If `updateSessionBar()` already handles this, just ensure it also sets `display:inline` when text is non-empty and `display:none` when text is empty or "No active session".

- [ ] **Step 3: Verify in browser**

On initial load, the session bar should show only the upload icon, "View / Edit Prompts", and "Load session". No "No active session" text. When a session is loaded or named, the session name should appear.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "fix: hide empty session status on initial load"
```

---

### Task 5: Smart Time Estimate

**Files:**
- Modify: `index.html:3386-3391` (JS — `updateEpTimer()` function)
- Modify: `index.html:1781` (HTML — timer estimate span)

- [ ] **Step 1: Add an ID to the estimate text span**

Find the timer HTML (~line 1781):
```html
    <span>Estimated total: 4&ndash;5 minutes</span>
```

Replace with:
```html
    <span id="ep-estimate">Estimated total: 4&ndash;5 minutes</span>
```

- [ ] **Step 2: Update `updateEpTimer()` to handle overruns**

Find lines 3386–3391:
```javascript
function updateEpTimer(){
  if(!epStartTime)return;
  const secs=Math.floor((Date.now()-epStartTime)/1000);
  const m=Math.floor(secs/60),s=secs%60;
  document.getElementById('ep-elapsed').textContent=m+':'+(s<10?'0':'')+s;
}
```

Replace with:
```javascript
function updateEpTimer(){
  if(!epStartTime)return;
  const secs=Math.floor((Date.now()-epStartTime)/1000);
  const m=Math.floor(secs/60),s=secs%60;
  document.getElementById('ep-elapsed').textContent=m+':'+(s<10?'0':'')+s;
  const est=document.getElementById('ep-estimate');
  if(est){
    if(secs>420)est.textContent='Almost done \u2014 finalising';
    else if(secs>300)est.textContent='Taking a little longer than usual';
  }
}
```

- [ ] **Step 3: Verify in browser**

Run an express analysis. Before 5 minutes: "Estimated total: 4–5 minutes". After 5 minutes: "Taking a little longer than usual". After 7 minutes: "Almost done — finalising".

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat: smart time estimate handles overruns gracefully"
```

---

### Task 6: Add Informative Messages to Rotation Pool

**Files:**
- Modify: `index.html:3336-3344` (JS — `EP_MESSAGES` array)

- [ ] **Step 1: Expand the EP_MESSAGES array**

Find lines 3336–3344:
```javascript
const EP_MESSAGES=[
  {icon:'☕',text:'Go grab a coffee — this will take a few minutes.'},
  {icon:'📖',text:'Your project is assessed against 12 operational standards and 25 key questions from the OST Manual.'},
  {icon:'⏳',text:'Don\'t worry, the wait will be worth it.'},
  {icon:'🌍',text:'In FCV settings, nothing good happens quickly.'},
  {icon:'📚',text:'We\'re drawing on the FCV Playbook for insights on relevant practices and operational tools.'},
  {icon:'🎯',text:'Stage 3 aligns recommendations with core priorities from the WBG FCV Strategy.'},
  {icon:'💡',text:'Tip: You can review each stage\'s findings once the analysis is complete.'}
];
```

Replace with:
```javascript
const EP_MESSAGES=[
  {icon:'☕',text:'Go grab a coffee — this will take a few minutes.'},
  {icon:'📖',text:'Your project is assessed against 12 operational standards and 25 key questions from the OST Manual.'},
  {icon:'⏳',text:'Don\'t worry, the wait will be worth it.'},
  {icon:'🌍',text:'In FCV settings, nothing good happens quickly.'},
  {icon:'📚',text:'We\'re drawing on the FCV Playbook for insights on relevant practices and operational tools.'},
  {icon:'🎯',text:'Stage 3 aligns recommendations with core priorities from the WBG FCV Strategy.'},
  {icon:'💡',text:'Tip: You can review each stage\'s findings once the analysis is complete.'},
  {icon:'🔍',text:'Searching trusted sources for country-specific FCV context.'},
  {icon:'📋',text:'Cross-referencing against 12 OST operational standards.'},
  {icon:'🏛️',text:'Aligning recommendations with WBG FCV Strategy priorities.'},
  {icon:'📐',text:'Tailoring guidance to your project\'s lifecycle stage.'},
  {icon:'🧰',text:'Drawing on FCV Playbook for operational tools and flexibilities.'}
];
```

- [ ] **Step 2: Verify in browser**

Run an express analysis and watch the rotating messages. Both casual and informative messages should appear in rotation.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add informative messages to express progress rotation pool"
```

---

### Task 7: Shorten Bottom Note

**Files:**
- Modify: `index.html:1787` (HTML — bottom note text)

- [ ] **Step 1: Replace the bottom note**

Find line 1787:
```html
  <div class="ep-bottom-note">Keep this tab open. You&rsquo;ll be able to review each stage&rsquo;s findings once the analysis is complete.</div>
```

Replace with:
```html
  <div class="ep-bottom-note">Do not close or refresh this tab.</div>
```

- [ ] **Step 2: Verify in browser**

The bottom note during express progress should show only "Do not close or refresh this tab." in small muted text.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: shorten express progress bottom note"
```

---

### Task 8: Informative Sub-Step Status on Stage Cards

**Files:**
- Modify: `index.html:2398-2412` (JS — SSE event handling in `runExpress()`)

- [ ] **Step 1: Update research_status and preprocess handlers to show sub-steps**

The SSE handlers for `research_status` and `preprocess` events already update `ep-summary-1`. Find the `research_status` handler (~lines 2398–2406). It already sets the summary HTML with spinner + text. Verify the messages are user-friendly:

- `'extracting_country'` → "Identifying project country…"
- `'searching'` → "Searching FCV context: [country]…"
- `'extracting_sector'` (if exists) → "Identifying project sector…"
- `'cached'` → "Loading cached FCV research…"
- `'complete'` → "Research complete — generating analysis…"

These are already informative based on the code found. The key improvement is to also show the **country name** once identified. Check if the `research_status` event with status `'searching'` includes the country name — the backend sends `{research_status: 'searching', country: 'Honduras'}`.

Find the handler and ensure the country name is displayed:
```javascript
// Around line 2400-2403 — verify this pattern exists:
if(d.research_status==='searching'){
  const countryText = d.country ? 'Searching FCV context: '+d.country+'…' : 'Searching FCV context…';
  sum1.innerHTML='<div class="ep-stage-spinner"><div class="ep-spinner-dot"></div><div class="ep-spinner-dot"></div><div class="ep-spinner-dot"></div><span>'+countryText+'</span></div>';
}
```

If the country is already shown (as seen in screenshots — "Searching FCV context: Honduras..."), this is already working. **No changes needed** — just verify.

- [ ] **Step 2: Verify in browser**

Run an express analysis. During Stage 1, the card should show sub-steps: "Identifying project country..." → "Searching FCV context: Honduras..." → "Generating analysis..."

- [ ] **Step 3: Commit (only if changes were made)**

```bash
git add index.html
git commit -m "feat: surface sub-step status on express stage cards"
```

---

### Task 9: Condense Stage 3 Instruction Callout

**Files:**
- Modify: `index.html:2089-2096` (JS — `CALLS` object, key `3`)

- [ ] **Step 1: Simplify the Stage 3 callout text**

Find the CALLS object (lines 2089–2096) and replace the key `3` entry:

Old:
```javascript
  3:{t:'Stage 3 of 3 · Recommendations Note — ready to copy',
     b:'<strong>What you are reading:</strong> This is the LLM-generated Recommendations Note for the Task Team Leader, drawing on the analysis from the previous two stages. Review it carefully before use — treat it as a well-informed draft, not a final expert assessment. You can still refine specific sections using the query box. When satisfied, click <strong>Copy all</strong> and paste into your Word document.'}
```

New:
```javascript
  3:{t:'Stage 3 of 3 · Recommendations Note — ready to copy',
     b:''}
```

This keeps the bold title but removes the verbose explanation body. The AI disclaimer block below already covers the caveats.

- [ ] **Step 2: Verify in browser**

After Stage 3 completes, the green callout should show only "Stage 3 of 3 · Recommendations Note — ready to copy" with no additional paragraph.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: condense stage 3 callout to single title line"
```

---

### Task 10: Remove S/R Definition Box from Stage 3

**Files:**
- Modify: `index.html:2964` (JS — Stage 3 rendering in `renderOut()`)

- [ ] **Step 1: Remove buildSRDefinitionBox from Stage 3 output**

Find line 2964 where Stage 3 content is assembled. It currently includes `buildSRDefinitionBox(true)`. Remove that call.

Find the line that looks like:
```javascript
_outBodyContent=buildSRDefinitionBox(true)+_stageBadge+md(_pre)+...
```

Replace `buildSRDefinitionBox(true)+` with just an empty string (remove the function call). Keep `_stageBadge` and everything after it.

The tag legend (compact reference for [S], [R], [S+R] badges) should remain if it's rendered separately. Check if `buildTagLegend()` is called for Stage 3 — if so, keep it.

- [ ] **Step 2: Verify in browser**

After Stage 3 completes, there should be no S/R definition box at the top of the output. The "Recommendations tailored for PAD stage" badge and the AI disclaimer should appear directly. If a tag legend exists, it should still show.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: remove repeated S/R definition box from stage 3 output"
```

---

### Task 11: Hide "view prompt" on Stage Headings

**Files:**
- Modify: `index.html:2976-2978` (JS — view prompt button in renderOut)

- [ ] **Step 1: Remove the view prompt button from stage headings**

Find lines 2976–2978 in the `renderOut()` template:
```javascript
<button class="view-prompt-link ${sessionPrompts[stage]?'modified':''}" onclick="openPromptModal(${stage})" title="${sessionPrompts[stage]?'Prompt modified for this session':'View the prompt used for this stage'}">
  ${sessionPrompts[stage]?'⚑ prompt (modified)':'view prompt'}
</button>
```

Replace with an empty string. Remove the entire `<button>` element. Users can still access prompts via "View / Edit Prompts" in the session bar.

- [ ] **Step 2: Verify in browser**

Stage headings (e.g., "3 Recommendations Note") should no longer show a "view prompt" link next to them. The "View / Edit Prompts" button in the session bar should still work.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: remove view prompt link from stage headings"
```

---

### Task 12: Priority Stepper Text Readability

**Files:**
- Modify: `index.html:701` (CSS — `.ps-label` rule)

- [ ] **Step 1: Bump font size and add ellipsis handling**

Find line 701:
```css
.ps-label{font-size:9px;color:var(--muted);font-weight:600;text-transform:uppercase;letter-spacing:.02em;line-height:1.35;word-break:break-word;white-space:normal;}
```

Replace with:
```css
.ps-label{font-size:10px;color:var(--muted);font-weight:600;text-transform:uppercase;letter-spacing:.02em;line-height:1.35;max-height:3.9em;overflow:hidden;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;word-break:break-word;white-space:normal;}
```

Changes: `font-size: 9px` → `10px`, add `max-height:3.9em` (3 lines × 1.3 line-height), `-webkit-line-clamp:3` for ellipsis after 3 lines.

- [ ] **Step 2: Verify in browser**

Priority stepper tabs should show slightly larger text. Long titles should be clamped at 3 lines with ellipsis. Full title visible on hover (the `title` attribute is already set in `renderPriorityStepper()` at line 3874).

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "fix: improve priority stepper text readability"
```

---

### Task 13: Final Verification

- [ ] **Step 1: Full walkthrough in Chrome**

1. Open the app — verify landing page: styled upload zones, correct mode badges, trimmed hero, clean session bar
2. Upload a test document and run Express Analysis
3. Verify progress screen: smart timer, mixed messages, shortened bottom note
4. After completion, verify Stage 3: no S/R definition box, condensed callout, no "view prompt" links, readable priority stepper
5. Verify "View / Edit Prompts" in session bar still works
6. Test step-by-step mode briefly to confirm no regressions

- [ ] **Step 2: Final commit**

```bash
git add index.html
git commit -m "chore: complete UX polish pass (v7.5)"
```
