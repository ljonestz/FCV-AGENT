[pytest]
# The public Climate-FCV bank is a separately versioned Python project with
# its own pyproject.toml and test root. Run its tests from the submodule.
norecursedirs =
    .git
    .worktrees
    data/climate-fcv-country-bank
